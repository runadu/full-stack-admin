# ========================================
# Full-Stack 專案環境變數範本
# ========================================
# 請複製此檔為 .env 後再修改實際值
# 範例值僅供示範用途，請勿於正式環境使用
# ========================================

# ============================================================
# 1. SQL Server（Docker container 設定）
# ============================================================

# sa 密碼
# 若密碼包含特殊字元（例如 !、@、:、/ 等），
# 需提供 URL 編碼版本供 DATABASE_URL 使用
#
# 範例：ChangeMe123! → ChangeMe123%21
MSSQL_SA_PASSWORD=ChangeMe123!

# 預設資料庫名稱
MSSQL_DB=devdb

# Docker 對外對應的主機 Port（本機連線使用）
MSSQL_HOST_PORT=14330

# ============================================================
# 2. Backend（FastAPI 設定）
# ============================================================

# 執行環境（development 會自動 create_all 建立資料表）
ENV=development

# JWT 設定
# 正式環境請使用長度至少 32 字元以上的隨機字串
JWT_SECRET=ChangeMe_To_A_Long_Random_String_At_Least_32_Chars
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60
MASSIVE_API_KEY=
MASSIVE_STOCK_TICKERS=AAPL,MSFT,NVDA,TSLA,AMZN,GOOGL
MASSIVE_TIMEOUT_SECONDS=10
STOCKS_CACHE_TTL_SECONDS=60
STOCKS_CACHE_STALE_IF_ERROR_SECONDS=900

# ============================================================
# 3. DATABASE_URL（API 連線設定）
# ============================================================
#
# 整合測試模式（Backend + DB in Docker）：
#   使用 sqlserver:1433
# 本機開發模式（DB in Docker）：
#   改為 localhost:14330
#
# ⚠ 此處密碼應使用 URL 編碼版本（對應 MSSQL_SA_PASSWORD_URLENCODED ）

DATABASE_URL=mssql+pyodbc://sa:ChangeMe123%21@sqlserver:1433/devdb?driver=ODBC+Driver+18+for+SQL+Server&Encrypt=yes&TrustServerCertificate=yes
